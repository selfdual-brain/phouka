package com.selfdualbrain.simulator_engine

/**
  * "Leaders sequence" validator.
  *
  * Blockchain nodes follow rounds-based schedule. Length of a round if fixed (configurable).
  * The leader of every round is picked by a pseudo-random function.
  * Within a round only the leader of this round produces a block. Every other node is supposed to produce a ballot.
  */
package object leaders_seq {

}
